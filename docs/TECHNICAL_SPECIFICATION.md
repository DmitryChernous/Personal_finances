# Техническое задание на разработку улучшений

**Дата создания:** 2026-01-25  
**Основано на:** EXPERT_REVIEW.md  
**Цель:** Пошаговая реализация критичных и важных улучшений для полноценного использования инструмента

---

## Принципы разработки

- **Инкрементальная реализация:** каждый этап дает законченный результат
- **Независимость этапов:** можно реализовывать в любом порядке
- **Обратная совместимость:** изменения не ломают существующие данные
- **DoD (Definition of Done):** четкие критерии готовности для каждого этапа

---

## Этап 1: Бюджетирование 🔴 КРИТИЧНО

**Приоритет:** Высший  
**Сложность:** Средняя  
**Время:** 4-6 часов

### Цель
Добавить возможность планирования и контроля бюджета по категориям.

### Задачи

#### 1.1. Создать схему листа Budgets
- **Файл:** `src/Schema.js`
- **Структура:**
  - `Category` (string, required) — категория
  - `Subcategory` (string, optional) — подкатегория (если нужна)
  - `Period` (enum, required) — период: `month` или `year`
  - `PeriodValue` (string, required) — значение периода: `YYYY-MM` для месяца, `YYYY` для года
  - `Amount` (number, required) — запланированная сумма (бюджет)
  - `Active` (boolean, optional) — активен ли бюджет
  - `Description` (string, optional) — описание/примечание

#### 1.2. Добавить лист Budgets в Setup
- **Файл:** `src/Setup.js`
- **Действия:**
  - Создать лист `Budgets` (или `Бюджеты` для RU)
  - Установить заголовки согласно схеме
  - Применить форматирование (заморозить первую строку, автофильтр)
  - Добавить валидации:
    - `Period`: список `month`, `year`
    - `Category`: из справочника Categories
    - `Amount`: число > 0
    - `PeriodValue`: формат `YYYY-MM` или `YYYY`

#### 1.3. Создать модуль Budgets.js
- **Функции:**
  - `pfInitializeBudgets_(ss)` — инициализация листа
  - `pfCalculateBudgetFact_(category, subcategory, periodValue)` — расчет факта из Transactions
  - `pfGetBudgetStatus_(budgetRow)` — статус бюджета (в норме/превышен/критично)
  - `pfUpdateBudgetCalculations_(ss)` — обновление расчетов для всех бюджетов

#### 1.4. Добавить расчеты в лист Budgets
- **Колонки с формулами:**
  - `Fact` — факт из Transactions (SUMIFS по категории и периоду)
  - `Remaining` — остаток (Plan - Fact)
  - `Status` — статус:
    - `ok` — в норме (остаток > 20% от плана)
    - `warning` — предупреждение (остаток 0-20%)
    - `exceeded` — превышен (факт > план)
  - `PercentUsed` — процент использования (Fact / Plan * 100)

#### 1.5. Добавить в Reports секцию "Бюджеты"
- **Файл:** `src/Reports.js`
- **Содержимое:**
  - Таблица с бюджетами текущего месяца
  - Колонки: Категория, План, Факт, Остаток, Статус, % использования
  - Подсветка превышенных бюджетов

#### 1.6. Добавить в Dashboard виджет бюджетов
- **Файл:** `src/Dashboard.js`
- **Содержимое:**
  - KPI: "Бюджетов превышено", "Средний % использования"
  - Список категорий с превышением бюджета (если есть)

#### 1.7. Добавить пункт меню
- **Файл:** `src/Code.js`
- **Пункт:** "Обновить бюджеты" — вызывает `pfUpdateBudgetCalculations_`

### DoD (Definition of Done)
- ✅ Лист Budgets создается при Setup
- ✅ Можно задать бюджет по категории на месяц/год
- ✅ Автоматически рассчитывается факт из Transactions
- ✅ Показывается остаток и статус
- ✅ В Reports и Dashboard отображаются данные о бюджетах
- ✅ Превышенные бюджеты подсвечиваются

### Тестирование
- Создать несколько бюджетов
- Добавить транзакции в категории с бюджетом
- Проверить расчет факта и остатка
- Проверить статусы (ok/warning/exceeded)
- Проверить отображение в Reports и Dashboard

---

## Этап 2: Регулярные платежи 🔴 КРИТИЧНО

**Приоритет:** Высший  
**Сложность:** Средняя  
**Время:** 5-7 часов

### Цель
Автоматизировать создание регулярных транзакций (подписки, аренда, коммуналка).

### Задачи

#### 2.1. Создать схему листа RecurringTransactions
- **Файл:** `src/Schema.js`
- **Структура:**
  - `Name` (string, required) — название регулярного платежа
  - `Type` (enum, required) — тип транзакции: `expense`, `income`
  - `Frequency` (enum, required) — частота: `monthly`, `quarterly`, `yearly`, `weekly`
  - `DayOfMonth` (number, optional) — день месяца (1-31, для monthly/quarterly/yearly)
  - `DayOfWeek` (number, optional) — день недели (1-7, для weekly)
  - `StartDate` (date, required) — дата начала
  - `EndDate` (date, optional) — дата окончания (null = бессрочно)
  - `Account` (string, required) — счет
  - `AccountTo` (string, optional) — счет получателя (для transfer)
  - `Amount` (number, required) — сумма
  - `Currency` (string, required) — валюта
  - `Category` (string, optional) — категория
  - `Subcategory` (string, optional) — подкатегория
  - `Merchant` (string, optional) — контрагент
  - `Description` (string, optional) — описание
  - `Active` (boolean, optional) — активен ли
  - `LastCreated` (date, optional) — дата последнего создания транзакции

#### 2.2. Добавить лист RecurringTransactions в Setup
- **Файл:** `src/Setup.js`
- **Действия:**
  - Создать лист `RecurringTransactions` (или `Регулярные платежи`)
  - Установить заголовки
  - Применить форматирование и валидации

#### 2.3. Создать модуль RecurringTransactions.js
- **Функции:**
  - `pfInitializeRecurringTransactions_(ss)` — инициализация листа
  - `pfShouldCreateTransaction_(recurringTx, currentDate)` — проверка, нужно ли создать транзакцию
  - `pfCreateRecurringTransactions_(ss, startDate, endDate)` — создание транзакций за период
  - `pfGetNextDueDate_(recurringTx)` — следующая дата платежа

#### 2.4. Реализовать логику создания транзакций
- **Алгоритм:**
  1. Прочитать все активные регулярные платежи
  2. Для каждого проверить, нужно ли создать транзакцию:
     - Для monthly: если день месяца наступил и LastCreated < текущего месяца
     - Для quarterly: если прошло 3 месяца с LastCreated
     - Для yearly: если прошло 12 месяцев с LastCreated
     - Для weekly: если прошла неделя с LastCreated
  3. Создать транзакцию в листе Transactions
  4. Обновить LastCreated в RecurringTransactions

#### 2.5. Добавить пункт меню
- **Файл:** `src/Code.js`
- **Пункт:** "Создать регулярные платежи" — вызывает `pfCreateRecurringTransactions_`
- **Опция:** "Создать за период" — позволяет указать период (по умолчанию текущий месяц)

#### 2.6. Добавить в Dashboard виджет
- **Файл:** `src/Dashboard.js`
- **Содержимое:**
  - Список предстоящих платежей (на этой неделе/месяце)
  - Сумма предстоящих платежей

### DoD (Definition of Done)
- ✅ Лист RecurringTransactions создается при Setup
- ✅ Можно создать регулярный платеж с настройкой частоты
- ✅ Меню "Создать регулярные платежи" создает транзакции автоматически
- ✅ Созданные транзакции имеют правильную дату, сумму, категорию
- ✅ Не создаются дубликаты (проверка по LastCreated)
- ✅ В Dashboard показываются предстоящие платежи

### Тестирование
- Создать регулярный платеж (ежемесячно)
- Запустить "Создать регулярные платежи"
- Проверить, что транзакция создана с правильной датой
- Проверить, что повторный запуск не создает дубликат
- Проверить разные частоты (weekly, monthly, quarterly, yearly)

---

## Этап 3: Автокатегоризация 🟡 ВАЖНО

**Приоритет:** Высокий  
**Сложность:** Средняя  
**Время:** 4-5 часов

### Цель
Автоматически определять категорию транзакции по Merchant/Description.

### Задачи

#### 3.1. Создать схему листа CategoryRules
- **Файл:** `src/Schema.js`
- **Структура:**
  - `RuleName` (string, required) — название правила
  - `Pattern` (string, required) — паттерн для поиска (текст или regex)
  - `PatternType` (enum, required) — тип: `contains`, `startsWith`, `endsWith`, `regex`, `exact`
  - `Category` (string, required) — категория для назначения
  - `Subcategory` (string, optional) — подкатегория
  - `Priority` (number, optional) — приоритет (чем выше, тем раньше проверяется)
  - `Active` (boolean, optional) — активен ли
  - `ApplyTo` (enum, optional) — применять к: `merchant`, `description`, `both`

#### 3.2. Добавить лист CategoryRules в Setup
- **Файл:** `src/Setup.js`
- **Действия:**
  - Создать лист `CategoryRules` (или `Правила категорий`)
  - Установить заголовки и валидации

#### 3.3. Создать модуль CategoryRules.js
- **Функции:**
  - `pfInitializeCategoryRules_(ss)` — инициализация листа
  - `pfMatchCategoryRule_(merchant, description, rules)` — поиск подходящего правила
  - `pfApplyCategoryRules_(transaction)` — применение правил к транзакции
  - `pfAutoCategorizeTransaction_(transaction)` — автокатегоризация одной транзакции

#### 3.4. Интегрировать в импорт
- **Файл:** `src/Import.js`
- **Изменения:**
  - В `pfProcessDataBatch` после нормализации вызывать `pfApplyCategoryRules_`
  - Применять правила к Merchant и Description
  - Если найдено правило, устанавливать Category и Subcategory

#### 3.5. Интегрировать в ручной ввод
- **Файл:** `src/Validation.js`
- **Изменения:**
  - В `pfNormalizeTransactionRow_` после заполнения Merchant/Description вызывать автокатегоризацию
  - Если категория не задана, попытаться определить автоматически

#### 3.6. Добавить предзаполненные правила
- **Файл:** `src/CategoryRules.js` или `src/DemoData.js`
- **Правила:**
  - Примеры для популярных мерчантов (Пятёрочка → Продукты, и т.д.)
  - Можно добавить через Setup или отдельную функцию

#### 3.7. Добавить пункт меню
- **Файл:** `src/Code.js`
- **Пункт:** "Применить автокатегоризацию" — применяет правила ко всем транзакциям без категории

### DoD (Definition of Done)
- ✅ Лист CategoryRules создается при Setup
- ✅ Можно создать правило (паттерн → категория)
- ✅ При импорте транзакции автоматически получают категорию по правилам
- ✅ При ручном вводе можно применить автокатегоризацию
- ✅ Правила проверяются по приоритету
- ✅ Меню "Применить автокатегоризацию" работает для существующих транзакций

### Тестирование
- Создать правило "Пятёрочка" → "Продукты"
- Импортировать транзакцию с Merchant="Пятёрочка"
- Проверить, что категория установлена автоматически
- Проверить приоритеты (если несколько правил подходят)
- Проверить применение к существующим транзакциям

---

## Этап 4: Мультивалютность 🟡 ВАЖНО

**Приоритет:** Высокий (если работаете с разными валютами)  
**Сложность:** Средняя-Высокая  
**Время:** 6-8 часов

### Цель
Добавить поддержку курсов валют и пересчет в базовую валюту.

### Задачи

#### 4.1. Создать схему листа ExchangeRates
- **Файл:** `src/Schema.js`
- **Структура:**
  - `Date` (date, required) — дата курса
  - `Currency` (string, required) — валюта (USD, EUR, и т.д.)
  - `Rate` (number, required) — курс к базовой валюте (RUB)
  - `Source` (string, optional) — источник курса (manual, cbr_api, и т.д.)

#### 4.2. Добавить лист ExchangeRates в Setup
- **Файл:** `src/Setup.js`
- **Действия:**
  - Создать лист `ExchangeRates` (или `Курсы валют`)
  - Установить заголовки и форматирование

#### 4.3. Создать модуль ExchangeRates.js
- **Функции:**
  - `pfInitializeExchangeRates_(ss)` — инициализация листа
  - `pfGetExchangeRate_(currency, date)` — получить курс валюты на дату
  - `pfConvertToBaseCurrency_(amount, currency, date)` — пересчет в базовую валюту
  - `pfUpdateExchangeRatesFromCBR_(ss)` — обновление курсов из API ЦБ РФ (опционально)

#### 4.4. Добавить базовую валюту в Settings
- **Файл:** `src/Settings.js`
- **Изменения:**
  - Добавить настройку `BaseCurrency` (по умолчанию RUB)
  - Сохранять в листе Settings

#### 4.5. Обновить Reports для мультивалютности
- **Файл:** `src/Reports.js`
- **Изменения:**
  - Добавить колонку "В базовой валюте" в отчеты
  - Пересчитывать суммы в базовую валюту по курсу на дату транзакции
  - Показывать суммы в исходной валюте и в базовой

#### 4.6. Обновить Dashboard для мультивалютности
- **Файл:** `src/Dashboard.js`
- **Изменения:**
  - KPI показывать в базовой валюте
  - Добавить информацию о валютах в транзакциях

#### 4.7. Добавить пункт меню
- **Файл:** `src/Code.js`
- **Пункт:** "Обновить курсы валют" — обновляет курсы из API или позволяет ввести вручную

### DoD (Definition of Done)
- ✅ Лист ExchangeRates создается при Setup
- ✅ Можно ввести курсы валют вручную
- ✅ В Reports суммы пересчитываются в базовую валюту
- ✅ В Dashboard KPI показываются в базовой валюте
- ✅ Курсы применяются на дату транзакции (исторические курсы)
- ✅ Опционально: автоматическое обновление из API ЦБ РФ

### Тестирование
- Добавить транзакции в разных валютах
- Ввести курсы валют на даты транзакций
- Проверить пересчет в базовую валюту в Reports
- Проверить корректность расчетов при разных курсах

---

## Этап 5: Расширенная аналитика

**Приоритет:** Средний  
**Сложность:** Средняя  
**Время:** 4-5 часов

### Цель
Добавить сравнение периодов, прогнозы и средние значения.

### Задачи

#### 5.1. Добавить сравнение с предыдущим периодом в Reports
- **Файл:** `src/Reports.js`
- **Содержимое:**
  - Колонка "Предыдущий месяц" — доходы/расходы за прошлый месяц
  - Колонка "Изменение" — разница в абсолютных значениях
  - Колонка "Изменение %" — процент изменения
  - Подсветка: зеленый (улучшение), красный (ухудшение)

#### 5.2. Добавить средние значения
- **Файл:** `src/Reports.js`
- **Содержимое:**
  - Средний расход в день (текущий месяц)
  - Средний расход в месяц (за последние 3/6/12 месяцев)
  - Средний доход в месяц

#### 5.3. Добавить простой прогноз
- **Файл:** `src/Reports.js`
- **Алгоритм:**
  - Прогноз на следующий месяц = среднее за последние 3 месяца
  - Или: тренд (линейная регрессия по последним 6 месяцам)

#### 5.4. Добавить график динамики в Dashboard
- **Файл:** `src/Dashboard.js`
- **Содержимое:**
  - Line chart: доходы и расходы по месяцам (последние 12 месяцев)
  - Использовать Google Sheets Charts API

### DoD (Definition of Done)
- ✅ В Reports показывается сравнение с предыдущим месяцем
- ✅ Показываются средние значения (в день, в месяц)
- ✅ Есть простой прогноз на следующий месяц
- ✅ В Dashboard есть line chart динамики

### Тестирование
- Добавить транзакции за несколько месяцев
- Проверить расчет сравнения с предыдущим периодом
- Проверить средние значения
- Проверить отображение графика

---

## Этап 6: Быстрый ввод транзакций

**Приоритет:** Средний  
**Сложность:** Средняя  
**Время:** 5-6 часов

### Цель
Упростить ввод транзакций для новичков через сайдбар.

### Задачи

#### 6.1. Создать HTML сайдбар QuickEntry.html
- **Файл:** `src/QuickEntry.html`
- **Содержимое:**
  - Форма с полями: Дата, Тип, Счет, Сумма, Валюта, Категория, Описание
  - Кнопки: "Добавить", "Добавить и еще одну", "Отмена"
  - Валидация на клиенте

#### 6.2. Создать модуль QuickEntry.js
- **Функции:**
  - `pfShowQuickEntry()` — открыть сайдбар
  - `pfAddQuickTransaction_(data)` — добавить транзакцию из формы
  - Валидация данных перед добавлением

#### 6.3. Добавить пункт меню
- **Файл:** `src/Code.js`
- **Пункт:** "Быстрый ввод транзакции" — открывает сайдбар

#### 6.4. Интегрировать с автокатегоризацией
- При заполнении формы применять правила категоризации

### DoD (Definition of Done)
- ✅ Сайдбар открывается из меню
- ✅ Можно ввести транзакцию через форму
- ✅ Транзакция добавляется в лист Transactions
- ✅ Валидация работает
- ✅ Автокатегоризация применяется

### Тестирование
- Открыть сайдбар
- Ввести транзакцию
- Проверить, что она появилась в таблице
- Проверить валидацию (неправильные данные отклоняются)

---

## Этап 7: Экспорт и резервное копирование

**Приоритет:** Средний  
**Сложность:** Низкая-Средняя  
**Время:** 2-3 часа

### Цель
Добавить возможность экспорта данных и создания резервных копий.

### Задачи

#### 7.1. Создать модуль Export.js
- **Функции:**
  - `pfExportTransactions_(format)` — экспорт транзакций (CSV/JSON)
  - `pfExportAllData_(format)` — экспорт всех данных
  - `pfCreateBackup_()` — создание резервной копии (копия листа Transactions)

#### 7.2. Добавить пункты меню
- **Файл:** `src/Code.js`
- **Пункты:**
  - "Экспорт транзакций (CSV)"
  - "Экспорт транзакций (JSON)"
  - "Создать резервную копию"

#### 7.3. Реализовать экспорт CSV
- Формат: все колонки Transactions
- Кодировка: UTF-8
- Разделитель: запятая или точка с запятой (в зависимости от локали)

#### 7.4. Реализовать экспорт JSON
- Формат: массив объектов
- Включать все поля транзакции

#### 7.5. Реализовать резервное копирование
- Создать лист `Transactions_Backup_YYYYMMDD`
- Скопировать все данные из Transactions
- Сохранить форматирование

### DoD (Definition of Done)
- ✅ Можно экспортировать транзакции в CSV
- ✅ Можно экспортировать транзакции в JSON
- ✅ Можно создать резервную копию
- ✅ Экспортированные данные можно импортировать обратно (CSV)

### Тестирование
- Экспортировать транзакции в CSV
- Проверить формат файла
- Создать резервную копию
- Проверить, что данные сохранены

---

## Этап 8: Поиск и фильтры

**Приоритет:** Низкий  
**Сложность:** Низкая  
**Время:** 2-3 часа

### Цель
Улучшить поиск и фильтрацию транзакций.

### Задачи

#### 8.1. Создать модуль Search.js
- **Функции:**
  - `pfSearchTransactions_(query)` — поиск по описанию/мерчанту
  - `pfShowSearchDialog_()` — диалог поиска
  - `pfHighlightSearchResults_(results)` — подсветка найденных строк

#### 8.2. Добавить пункт меню
- **Файл:** `src/Code.js`
- **Пункт:** "Найти транзакции" — открывает диалог поиска

#### 8.3. Реализовать поиск
- Поиск по полям: Description, Merchant, Category
- Поддержка частичного совпадения
- Подсветка найденных строк
- Переход к первой найденной строке

### DoD (Definition of Done)
- ✅ Можно искать транзакции по тексту
- ✅ Найденные строки подсвечиваются
- ✅ Можно перейти к найденной транзакции

### Тестирование
- Поиск по описанию
- Поиск по мерчанту
- Проверить подсветку результатов

---

## Этап 9: Дополнительные графики

**Приоритет:** Низкий  
**Сложность:** Средняя  
**Время:** 3-4 часа

### Цель
Добавить line charts и bar charts для визуализации.

### Задачи

#### 9.1. Добавить line chart динамики в Dashboard
- **Файл:** `src/Dashboard.js`
- **Содержимое:**
  - График доходов и расходов по месяцам (line chart)
  - Использовать данные из Reports (месячный cashflow)

#### 9.2. Добавить bar chart сравнения
- **Файл:** `src/Dashboard.js`
- **Содержимое:**
  - Bar chart: сравнение текущего месяца с предыдущим
  - Доходы и расходы

#### 9.3. Добавить анализ по дням недели
- **Файл:** `src/Dashboard.js`
- **Содержимое:**
  - Bar chart: средние расходы по дням недели
  - Показывает, в какие дни тратится больше

### DoD (Definition of Done)
- ✅ В Dashboard есть line chart динамики
- ✅ Есть bar chart сравнения месяцев
- ✅ Есть анализ по дням недели

### Тестирование
- Проверить отображение графиков
- Проверить корректность данных на графиках

---

## Этап 10: Архивирование старых данных

**Приоритет:** Низкий  
**Сложность:** Низкая  
**Время:** 2-3 часа

### Цель
Оптимизировать производительность при большом количестве транзакций.

### Задачи

#### 10.1. Создать модуль Archive.js
- **Функции:**
  - `pfArchiveOldTransactions_(cutoffDate)` — архивирование транзакций старше даты
  - `pfCreateArchiveSheet_(ss)` — создание листа Transactions_Archive
  - `pfRestoreFromArchive_(ss)` — восстановление из архива (опционально)

#### 10.2. Добавить пункт меню
- **Файл:** `src/Code.js`
- **Пункт:** "Архивировать старые транзакции" — архивирует транзакции старше указанной даты

#### 10.3. Реализовать архивирование
- Переместить транзакции старше cutoffDate в Transactions_Archive
- Удалить из Transactions
- Сохранить форматирование и структуру

### DoD (Definition of Done)
- ✅ Можно архивировать старые транзакции
- ✅ Архив создается в отдельном листе
- ✅ Производительность улучшается (меньше строк в Transactions)

### Тестирование
- Создать транзакции с разными датами
- Запустить архивирование
- Проверить, что старые транзакции перемещены в архив
- Проверить, что новые транзакции остались

---

## Приоритизация этапов

### Критичные (для реального использования):
1. **Этап 1: Бюджетирование** 🔴
2. **Этап 2: Регулярные платежи** 🔴

### Важные (для удобства):
3. **Этап 3: Автокатегоризация** 🟡
4. **Этап 4: Мультивалютность** 🟡 (если нужна)
5. **Этап 5: Расширенная аналитика** 🟡

### Желательные (nice to have):
6. **Этап 6: Быстрый ввод** 🟢
7. **Этап 7: Экспорт и бэкапы** 🟢
8. **Этап 8: Поиск** 🟢
9. **Этап 9: Дополнительные графики** 🟢
10. **Этап 10: Архивирование** 🟢

---

## Общие требования для всех этапов

### Код
- Использовать существующие модули (Constants, ErrorHandler, DateUtils)
- Следовать архитектуре проекта
- Добавлять валидацию входных данных
- Использовать i18n для всех текстов

### Тестирование
- Каждый этап должен быть протестирован отдельно
- Тесты должны покрывать основные сценарии
- Edge cases должны быть обработаны

### Документация
- Обновить PROJECT_STATE.md после каждого этапа
- Обновить Help.js с инструкциями по новым функциям
- Добавить примеры использования

---

## Порядок реализации (рекомендуемый)

1. **Этап 1** (Бюджетирование) — критично, относительно просто
2. **Этап 2** (Регулярные платежи) — критично, средняя сложность
3. **Этап 3** (Автокатегоризация) — значительно улучшит UX
4. **Этап 5** (Расширенная аналитика) — улучшит аналитику
5. **Этап 6** (Быстрый ввод) — улучшит UX для новичков
6. **Этап 7** (Экспорт) — безопасность данных
7. **Этап 4** (Мультивалютность) — если нужна
8. Остальные этапы — по необходимости

---

## Примечания

- Все этапы независимы и могут реализовываться в любом порядке
- После каждого этапа рекомендуется тестирование и фиксация состояния
- Можно пропускать этапы, которые не нужны конкретному пользователю
- Приоритеты могут меняться в зависимости от потребностей пользователя
