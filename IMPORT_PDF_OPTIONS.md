# Варианты импорта данных из PDF файлов

## Проблема

Большинство банков предоставляют выписки в формате PDF, который сложно парсить программно из-за:
- Разных форматов макета у разных банков
- Текстового формата без структуры
- Необходимости распознавания текста (OCR) для сканированных PDF

## Варианты решения

### Вариант 1: Экспорт PDF в CSV/Excel (РЕКОМЕНДУЕТСЯ) ⭐

**Описание**: Пользователь экспортирует PDF в CSV/Excel через:
- Банковские приложения (многие банки позволяют экспортировать выписки в CSV)
- Онлайн-конвертеры PDF → Excel/CSV
- Ручное копирование данных из PDF в Excel

**Преимущества**:
- ✅ Простая реализация (используем существующий CSV импортер)
- ✅ Надежность (пользователь контролирует формат)
- ✅ Работает со всеми банками
- ✅ Не требует дополнительных API/сервисов

**Недостатки**:
- ⚠️ Требует ручного шага от пользователя
- ⚠️ Может быть медленным для больших выписок

**Реализация**: Уже реализовано через CSV импортер.

---

### Вариант 2: Парсинг PDF через Google Drive API

**Описание**: Использование Google Drive API для извлечения текста из PDF, затем парсинг текста.

**Преимущества**:
- ✅ Прямая работа с PDF файлами
- ✅ Не требует сторонних сервисов

**Недостатки**:
- ❌ Сложный парсинг (нужно распознавать структуру для каждого банка)
- ❌ Низкая надежность (форматы банков различаются)
- ❌ Требует создания парсера для каждого банка
- ❌ Не работает со сканированными PDF (нужен OCR)

**Реализация**: 
```javascript
// Пример извлечения текста из PDF
var file = DriveApp.getFileById(fileId);
var blob = file.getBlob();
var text = blob.getDataAsString(); // Работает только для текстовых PDF
// Затем нужен сложный парсинг текста...
```

**Статус**: Не рекомендуется для начальной реализации.

---

### Вариант 3: Сторонние сервисы (OCR + парсинг)

**Описание**: Использование внешних API для:
- OCR (распознавание текста из сканированных PDF)
- Парсинг структурированных данных

**Примеры сервисов**:
- Google Cloud Vision API (OCR)
- Adobe PDF Services API
- ABBYY FineReader API
- Специализированные сервисы для банковских выписок

**Преимущества**:
- ✅ Работает со сканированными PDF
- ✅ Может автоматически распознавать структуру

**Недостатки**:
- ❌ Требует API ключи и может быть платным
- ❌ Зависимость от внешних сервисов
- ❌ Сложная интеграция
- ❌ Проблемы с приватностью (данные уходят на внешние сервисы)

**Реализация**: Требует настройку API ключей и интеграцию.

**Статус**: Можно добавить позже как опциональную функцию.

---

### Вариант 4: Гибридный подход (рекомендуется для будущего)

**Описание**: Комбинация вариантов:
1. **Основной путь**: Экспорт PDF → CSV/Excel → импорт (Вариант 1)
2. **Опциональный путь**: Прямой импорт PDF для популярных банков (Вариант 2/3)

**Реализация**:
- Начать с Варианта 1 (CSV импорт) - уже реализовано
- Добавить поддержку конкретных банков по запросу:
  - Сбербанк (если есть структурированный формат)
  - Тинькофф (если есть API или структурированный формат)
  - Другие банки по мере необходимости

**Статус**: Рекомендуется для будущего развития.

---

## Рекомендация

**Для текущей реализации**: Использовать **Вариант 1** (экспорт PDF → CSV/Excel).

**Инструкция для пользователей**:
1. Откройте выписку в PDF
2. Экспортируйте в CSV/Excel через:
   - Банковское приложение (если доступно)
   - Онлайн-конвертер PDF → Excel
   - Ручное копирование в Excel
3. Импортируйте CSV/Excel файл через интерфейс импорта

**Для будущего**: Можно добавить поддержку конкретных банков, если:
- Банк предоставляет структурированный формат (JSON, XML, CSV)
- Есть официальный API
- Есть запросы от пользователей

---

## Текущая реализация

✅ **Реализовано**:
- CSV импортер с автоматическим определением колонок
- UI для импорта файлов
- Preview и Commit workflow
- Дедупликация транзакций
- Обработка ошибок парсинга (пометка на проверку)

📋 **В планах**:
- Поддержка Excel файлов (через конвертацию в CSV или прямой парсинг)
- Парсеры для конкретных банков (по запросу)
- Улучшенный маппинг колонок в UI
